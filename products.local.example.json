{
  "_comment": "Copy to products.local.json (gitignored) on the ONE machine that provisions boards. It is merged over products.json. Nothing here is a secret. Keys are minted by the Worker; the bench's provisioning token lives in ~/.config/valar-flasher/provision-token (never in this file), and nobody holds DEVICE_KEY_SECRET.",
  "products": {
    "Blipscope": {
      "provisioner": {
        "command": "C:/path/to/Blipscope/scripts/provision_one.py",
        "args": ["--env", "blipscope-s3-128", "--verify-url", "https://scopes.valarsystems.com"],
        "log": "C:/path/to/Blipscope/provisioned.csv"
      },
      "variants": {
        "Kit S3 1.28in (s3-128)": {
          "_release_tag": "set to a prerelease tag to flash from it instead of releases/latest",
          "_local_build": "set to a directory written by Blipscope's scripts/flash_manifest.py --out to flash a LOCAL BUILD (labelled; see README)"
        }
      }
    }
  }
}
